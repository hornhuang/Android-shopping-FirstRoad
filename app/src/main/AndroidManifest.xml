<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstroad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".itemdetails.ClopediaDetailMineActivity"></activity>
        <activity android:name=".itemdetails.OrdersDetailsActivity" />
        <activity android:name=".itemdetails.ClopediaDetailActivity" />
        <activity android:name=".itemdetails.RoutesPlayDetailsActivity" />
        <activity android:name=".itemdetails.RoutesDetailsActivity" />
        <activity android:name=".itemdetails.GoodsDetailsActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>
